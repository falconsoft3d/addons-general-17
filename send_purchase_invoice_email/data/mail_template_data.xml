<?xml version="1.0" ?>
<odoo>

    <data noupdate="0">

    <!-- Mail template are declared in a NOUPDATE block
         so users can freely customize/delete them -->

        <record id="email_template_purchase_invoice_es" model="mail.template">
            <field name="name">Enviar Factura</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="email_from">{{ (object.invoice_user_id.email_formatted or object.company_id.email_formatted or user.email_formatted) }}</field>
            <field name="partner_to">{{ object.partner_id.id }}</field>
            <field name="subject">{{ object.company_id.name }} Invoice (Ref {{ object.name or 'n/a' }})</field>
            <field name="description">Sent to customers with their invoices in attachment</field>
            <field name="body_html" type="html">
                    <div style="margin: 0px; padding: 0px;">
                        <p style="margin: 0px; padding: 0px; font-size: 13px;">
                            Hola
                            <t t-if="object.partner_id.parent_id">
                                <t t-out="object.partner_id.name or ''">Brandon Freeman</t> (<t t-out="object.partner_id.parent_id.name or ''">Azure Interior</t>),
                            </t>
                            <t t-else="">
                                <t t-out="object.partner_id.name or ''">Brandon Freeman</t>,
                            </t>
                            <br /><br />

                            Adjunto le enviamos la factura:<br/>
                            <span style="font-weight:bold;">REFERENCIAS</span><br/>
                            Número de factura: <span style="font-weight:bold;" t-out="object.name or ''">INV/2021/05/0005</span><br/>
                            Total de factura: <span style="font-weight:bold;" t-out="format_amount(object.amount_total, object.currency_id) or ''">$ 143,750.00</span><br/>
                            Fecha de factura: <span style="font-weight:bold;" t-out="object.invoice_date or ''">2021-05-01</span><br/>
                            Referencia de factura: <span style="font-weight:bold;" t-out="object.invoice_origin or ''">SUB003</span><br/>

                            <br />
                            Un saludo,<br/>
                            <t t-out="object.invoice_user_id.signature or ''">--<br/>Mitchell Admin</t>
                        </p>
                    </div>
            </field>
            <field name="report_template_ids" eval="[(4, ref('account.account_invoices'))]"/>
            <field name="lang">${object.partner_id.lang}</field>
        </record>



    </data>
</odoo>
